
# to turn string of text into comma separated text
gawk '{$1=$1}1' FPAT='.{1}' OFS=, input.file  


# to compile all .tex to .pdf
for i in 'the-four-vows*.tex'; do xelatex -synctex=1 -interaction=nonstopmode $i; done    

